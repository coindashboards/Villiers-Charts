{
  "name": "coindashboards-heatmaps",
  "version": "1.0.0",
  "description": "Package that handles heatmaps tab on coindashboards.com webpage",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "milkyklim",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.0.0",
    "mongoose": "^5.2.9",
    "node-binance-api": "^0.8.1"
  }
}
